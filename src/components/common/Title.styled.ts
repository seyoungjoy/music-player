import styled from '@emotion/styled';

import { COLOR } from '../../constant/color';

export const S = {
  H1: styled.h1`
    font-weight: bold;
    font-size: 30px;
    color: ${COLOR.DARK};
  `,
};
