import styled from '@emotion/styled';

import { COLOR } from '../../constants/color';

export const S = {
  H1: styled.h1`
    font-weight: bold;
    font-size: 20px;
    color: ${COLOR.DARK};
  `,
};
