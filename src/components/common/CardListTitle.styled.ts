import styled from '@emotion/styled';

import { TEXT } from '../../constants/color';

export const S = {
  H1: styled.h1`
    margin-bottom: 20px;
    font-weight: bold;
    font-size: 20px;
    color: ${TEXT.PRIMARY};
  `,
};
