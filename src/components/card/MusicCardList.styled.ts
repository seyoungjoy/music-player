import styled from '@emotion/styled';

const S = {
  Wrapper: styled.ul`
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(130px, auto));
    gap: 10px;
  `,
};

export default S;
